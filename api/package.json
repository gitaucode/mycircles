{
  "name": "my-circles-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply my-circles-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply my-circles-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260629.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.24.0"
  }
}
